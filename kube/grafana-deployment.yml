apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: bryce2
  name: grafana-deployment
  labels:
    app: grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: grafana
  template:
    metadata:
      labels:
        app: grafana
    spec:
      containers:
      - name: grafana
        image: grafana/grafana:9.4.7
        ports:
        - containerPort: 3000
        env:
        - name: GF_AUTH_ANONYMOUS_ENABLED
          value: "true"
        - name: GF_AUTH_ANONYMOUS_ORG_ROLE
          value: Admin
        - name: GF_AUTH_DISABLE_LOGIN_FORM
          value: "true"
        volumeMounts:
        - name: datasources
          mountPath: /etc/grafana/provisioning/datasources/datasources.yml
          subPath: grafana-datasource.yml
      volumes:
      - name: datasources
        configMap:
          name: grafana-datasource-config
      #   volumeMounts:
      #   - name: grafana-config
      #     mountPath: /etc/grafana-config.yaml
      #     subPath: grafana-config.yaml
      # volumes:
      # - name: grafana-config
      #   configMap:
      #     name: grafana-config