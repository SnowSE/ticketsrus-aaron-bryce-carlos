apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: bryce2
  name: grafana-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: grafana.bryce-k8s.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: grafana-service
            port:
              number: 80